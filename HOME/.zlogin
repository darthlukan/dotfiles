# Uncomment the code below if using zsh
# and you want to start X automatically on
# login from TTY. Will use the DE/WM
# supplied in ~/.xinitrc

#if [[ -z $DISPLAY ]]; then
#    if [[ $(tty) = /dev/tty1 ]]; then
#        exec startx
#    fi
#fi
